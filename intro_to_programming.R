weight_kg <- c(55,25,12)

#convert weight to lbs
2.2 * weight_kg


#Set relative directory ----
# 4 dashes adds outlione (topright of source view)
#tab pulls in top csv file

chem<-read.csv("data/BGchem2008data.csv")

# run these in console str(chem), View(chem)















